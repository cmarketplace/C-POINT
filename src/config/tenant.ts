/**
 * 이 몰이 어느 기관 것인가 — 기관 종속값은 전부 여기 모은다.
 *
 * KCL MRO 는 같은 값들이 29개 파일에 문자열로 흩어져 있었다. FITI 를 세울 때 그것을
 * 이 파일 하나로 접었고, 씨마켓몰은 그 구조를 그대로 물려받는다 — 몰을 하나 더 세울 때
 * 갈아 끼우는 곳은 여기와 `globals.css` 의 브랜드 색, 그리고 `.env` 셋뿐이다.
 *
 * 여기 없는 것: **세모 접속 정보**(`SEMO_API_BASE` / `SEMO_API_KEY` / `STOREFRONT_SLUG`).
 * 그건 환경마다 다른 값이라 `.env` 가 갖는다 — 코드에 적으면 스테이징에서 조용히 어긋난다.
 *
 * 이름: C-POINT 몰은 2026-08-30 결정으로 **«씨마켓몰»** 이라 부른다(포인트를 쓰지 않기로
 * 해서). 저장소·디렉터리 이름은 여전히 c-point 다.
 */

export const TENANT = {
  /** 화면 곳곳의 몰 이름 */
  shopName: '씨마켓몰',

  /** 운영 주체 정식 명칭. 안내 문구·메타데이터에 쓴다 */
  orgName: '씨마켓',

  /** 안전결제의 계약 상대 — 결제 화면·영수증에 그대로 적힌다 */
  safePaymentCounterpart: '씨마켓플레이스(주)',

  /** 상세 화면의 가격 라벨 — 「이 몰 전용가」임을 밝히는 자리 */
  priceLabel: '씨마켓몰 판매가',

  /**
   * 상품 상세의 신뢰 배지 문구.
   * 지어낸 수치를 넣지 않는다 — 세모 피드가 실제로 보증하는 것만 적는다.
   */
  productBadges: ['세모 물품관리시스템 표준 품목', '씨마켓몰 승인 공급사 단가'],

  /** «이 가격보다 더 받고 싶다면» — 몰에서 씨마켓 공고 등록으로 나가는 문 */
  bidRegisterUrl: 'https://www.c-market.net',
} as const
