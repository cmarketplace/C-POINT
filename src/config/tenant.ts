/**
 * 이 몰이 어느 기관 것인가 — 기관 종속값은 전부 여기 모은다.
 *
 * KCL MRO 는 같은 값들이 29개 파일에 문자열로 흩어져 있었다. FITI 를 세울 때 그것을
 * 이 파일 하나로 접었고, C-POINT 는 그 구조를 그대로 물려받는다 — 몰을 하나 더 세울 때
 * 갈아 끼우는 곳은 여기와 `globals.css` 의 브랜드 색, 그리고 `.env` 셋뿐이다.
 *
 * 여기 없는 것: **세모 접속 정보**(`SEMO_API_BASE` / `SEMO_API_KEY` / `STOREFRONT_SLUG`).
 * 그건 환경마다 다른 값이라 `.env` 가 갖는다 — 코드에 적으면 스테이징에서 조용히 어긋난다.
 *
 * ⚠️ 아래 이름·문구는 **확정 전 임시값**이다. C-POINT 의 정식 몰 이름과 운영 주체가
 * 정해지면 이 파일만 고치면 화면 전체가 따라온다.
 */

export const TENANT = {
  /** 화면 곳곳의 몰 이름 */
  shopName: 'C-POINT',

  /** 운영 주체 정식 명칭. 안내 문구·메타데이터에 쓴다 */
  orgName: '씨마켓',

  /** 상세 화면의 가격 라벨 — 「이 몰 전용가」임을 밝히는 자리 */
  priceLabel: 'C-POINT 전용 판매가',

  /**
   * 상품 상세의 신뢰 배지 문구.
   * 지어낸 수치를 넣지 않는다 — 세모 피드가 실제로 보증하는 것만 적는다.
   */
  productBadges: ['세모 물품관리시스템 표준 품목', 'C-POINT 승인 공급사 단가'],
} as const
